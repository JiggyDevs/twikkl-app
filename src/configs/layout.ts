import { StyleSheet } from "react-native";

export const CommonViewStyles = StyleSheet.create({
  page: {
    flex: 1,
  },
  centered: {
    alignItems: "center",
    justifyContent: "center",
  },
});
